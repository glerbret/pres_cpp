#include <iostream>
#include <cmath>

int main()
{
  std::cout << std::lerp(10, 20, 0) << "\n";
  std::cout << std::lerp(10, 20, 0.1) << "\n";
  std::cout << std::lerp(10, 20, 0.2) << "\n";
  std::cout << std::lerp(10, 20, 0.3) << "\n";
  std::cout << std::lerp(10, 20, 0.4) << "\n";
  std::cout << std::lerp(10, 20, 0.5) << "\n";
  std::cout << std::lerp(10, 20, 0.6) << "\n";
  std::cout << std::lerp(10, 20, 0.7) << "\n";
  std::cout << std::lerp(10, 20, 0.8) << "\n";
  std::cout << std::lerp(10, 20, 0.9) << "\n";
  std::cout << std::lerp(10, 20, 1) << "\n";
}
